enum ShellRoute {
  dashboard,
  quotationComposer,
  quotations,
  invoices,
  products,
  parties,
  reports,
  settings,
}

abstract final class AppRoutePaths {
  static const resetPassword = '/reset-password';
}
